问:
这句代码到底是什么意思？为什么一定要加个`new`？ 
if (BTT="") 
   BTT := new BeautifulToolTip()

AI 回答:
这段代码的目的是创建 `BeautifulToolTip` 类的一个实例，并确保只创建一次。关键在于 `new` 关键字和初始化时机。让我详细解释一下：

1. **检查是否初始化**：`if (BTT="")` 这行代码检查变量 `BTT` 是否未被赋值或为空。`BTT` 变量用于存储 `BeautifulToolTip` 类的实例。

2. **创建实例**：如果 `BTT` 变量未被赋值或为空，则执行 `BTT := new BeautifulToolTip()` 这行代码。这行代码的作用是使用 `BeautifulToolTip` 类创建一个新的实例，并将该实例赋值给变量 `BTT`。`new` 关键字用于调用类的构造函数，生成该类的一个对象实例。

3. **只创建一次**：通过这样的检查和赋值，你确保了 `BTT` 变量在整个程序的生命周期中只会被创建一次。后续对 `BTT` 的使用将直接引用已经创建好的实例，而不会重复创建新的实例，节省了内存和提高了效率。

这个模式通常被称为**单例模式**（Singleton Pattern），用于确保一个类只有一个实例，并提供一个全局访问点。

如果你有更多关于这段代码或 `new` 关键字的疑问，请随时告诉我！我很乐意帮助你进一步理解。

效率测试结果:
发现现在的 V2版本 比原作者的 V1版本, 它的CPU占用率提高10倍不止. 不知道是否有方法改良它嗎?

之前我尝试修改它成为 V2 版本的时候, 发现 `BTT := new BeautifulToolTip()` 这句代码的改变, 可能是导致占用率提高的原因, 不知道是否有其他方法可以优化它.