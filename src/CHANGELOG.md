<!-- InputTip -->

# 版本更新日志

## 3.6.8

- 为 `状态提示 - 窗口边框` 添加 `哪些窗口状态下显示`
- 为 `规则管理 - 窗口` 和 `规则管理 - 热键` 添加触发行为
  - `状态切换 - 中英交替 (IME)`
  - `状态切换 - 中英交替 (LShift)`
  - `状态切换 - 中英交替 (RShift)`
  - `状态切换 - 中英交替 (CtrlSpace)`
- 改进了 `IME`，提高了状态识别的准确性
- 其他的优化和修复

## 3.6.7

- 在 `更多设置` 中添加 `菜单的字体大小`
- 为 `状态提示 - 悬浮小窗` 添加 `哪些窗口状态下显示`
- 添加 `显示/隐藏光标捕获模式`
- 在 `关于` 中添加运行环境信息
- 优先使用 `DwmGetWindowAttribute` 获取准确的窗口边界
- 版本更新包含运行时
- 改进了 `IME`
- 优化了菜单布局
- 其他的优化和修复

## 3.6.6

- 为 `状态提示 - 窗口边框` 新增窗口最大化与全屏状态下的独立边框选项
- 当 `中文输入法的识别模式` 中的 `自定义` 规则列表为空时，如果选择了 `自定义`，将回退为 `通用` 实现
- 修复了一个变量初始化错误的问题
- 优化了 `状态提示 - 悬浮小窗` 的菜单布局
- 其他的优化和修复

## 3.6.5

- 启用状态提示方案后，始终会在 InputTip 自身的菜单界面中显示，不受黑白名单影响
- 修复了 JAB 子进程中 `光标捕获模式` 中的偏移量无效的问题
- 修复了鼠标样式文件没有忽略大小写的问题
- 修复了 `中文输入法的状态识别模式` 的 `自定义` 设置错误
- 其他的优化和修复

## 3.6.4

- 为 `状态提示 - 鼠标样式` 添加 `只加载 IBeam 样式`
- 添加了更精确的日文和韩文状态切换
- 改进了 `状态提示 - 窗口边框`，当未给置顶窗口指定边框颜色时，回退到状态提示
- 改进了 `IME`
- 修复了 `光标捕获模式` 中的偏移量无效的问题
- 优化了菜单布局
- 优化了版本更新
- 其他的优化和修复

## 3.6.3

- 改进了 `中文输入法的状态识别模式` 下的 `通用`
  - 现在引入了纯数据流差分特征学习机制
  - 动态捕获各类输入法的状态码与转换码跳变特征，实现主流输入法全自适应兼容
- 移除了符号的 DPI 缩放
- 改进了对输入光标位置的捕获逻辑
- 优化了菜单布局
- 其他的优化和修复

## 3.6.2

- 为 `状态提示 - 悬浮小窗` 添加 `仅焦点所在屏幕显示`
- 所有的符号样式独立于状态，可以分别设置
- 修复了屏幕 DPI 相关的误差问题
- 优化了菜单布局
- 其他的优化和修复

## 3.6.1

- 允许为 `状态提示 - 光标符号` 中的 `光标捕获模式` 设置特定的偏移量
- 优化了 `IME`，当状态无法识别时，沿用上一次状态
- 修复了多个规则使用相同行为可能失效的问题
- 其他的优化和修复

## 3.6.0

- 在 `输入法相关` 中添加 `键盘切换时保持大写锁定`
- 为规则添加多种行为
  - `无操作`
  - `应用控制 - 暂停/恢复`
  - `窗口状态 - 切换置顶`
- 区分中文键盘和英文键盘的英文状态，现在可以识别六种状态
  - 中文状态 (中文键盘)
  - 英文状态 (中文键盘)
  - 大写锁定 (CapsLock)
  - 英文键盘
  - 日文键盘
  - 韩文键盘
- 不再使用精确时间作为规则 ID，由年月日加随机字符替代
- 改进了 `状态提示 - 光标符号` 中的 `光标捕获模式`
- 其他的优化和修复

## 3.5.3

- 重命名了一些规则的触发行为
  - `暂停` > `应用控制 - 暂停`
  - `退出` > `应用控制 - 退出`
  - `重启` > `应用控制 - 重启`
- 现在采用伪暂停，为规则添加 `应用控制 - 恢复`
- 修复了 `应用控制 - 暂停` 没有清除所有状态提示的问题
- 其他的优化和修复

## 3.5.2

- 修复了 `状态提示 - 鼠标符号` 中的 `黑名单` 和 `白名单` 无效的问题
- 为 `状态提示 - 鼠标符号` 补充了 `延时隐藏` 配置项
- 其他的优化和修复

## 3.5.1

- 修复了热键规则无效的问题
- 修复了使用 `Shift + 其他键`（如 `Shift + 1`）时，抬起 Shift 会意外触发热键规则的问题
- 修复了 `状态提示 - 光标符号` 中的 `窗口偏移量` 实时预览失效的问题
- 其他的优化和修复

## 3.5.0

- 新增状态提示方案 `状态提示 - 窗口边框`，通过彩色边框直观呈现当前输入状态
- 新增状态提示方案 `状态提示 - 鼠标符号`，从 `符号方案` 中独立拆分，`符号方案` 更名为 `状态提示 - 光标符号`
- 将 `状态切换 - 窗口` 和 `状态切换 - 热键` 整合为 `规则管理 - 窗口` 和 `规则管理 - 热键`，旧的 `匹配标题` 和 `匹配模式` 重构为附加条件
- 现在 `规则管理` 使用统一的条件匹配和行为调度
  - 条件: 可以使用窗口匹配、文本监控、热键监控、空闲计时器等条件
  - 行为: 包含状态切换、键盘切换、热键动态注册、窗口置顶等行为
- 状态提示方案更名
  - `状态提示 - 符号方案` > `状态提示 - 光标符号`
  - `状态提示 - 鼠标方案` > `状态提示 - 鼠标样式`
  - `状态提示 - 浮窗方案` > `状态提示 - 悬浮小窗`
- 规则中的窗口匹配均支持正则表达式，新增 `窗口类名` 作为可选匹配条件
- `状态提示 - 光标符号` 的 `光标捕获模式` 支持链式选择，依次尝试匹配
- `按键次数统计` 的数据现在会写入数据目录下的 `stats.ini`
- 重构偏移量计算方式，统一使用物理坐标，偏移量配置使用逻辑坐标
- 优化 `状态提示 - 悬浮小窗` 和 `状态提示 - 光标符号` 的菜单布局与操作逻辑
- 优化调色盘 🎨 的操作逻辑，左键单击设置颜色，右键单击清除颜色
- 优化英文版菜单字体，从等宽字体改为微软雅黑
- 改进 `IME` 的兼容性
- 移除动画效果 `缩放` 以提升稳定性
- 其他的优化和修复

## 3.4.0

- 为 `浮窗方案` 添加更多可选择的显示位置
- 为 `开机自启动` 添加检查
- 修复了热键注册
- 其他的优化和修复

## 3.3.0

- 为 `浮窗方案` 添加 `黑名单` 和 `白名单`
- 为 `浮窗方案` 添加 `进程切换时重显`，并将 `窗口切换时重显` 更名为 `窗口和标题切换时重显`
- 优化了 `浮窗方案` 的菜单布局
- 将 `圆角样式` 更名为 `边角样式`
- 优化了 zip 版本更新和文件缺失检查
- 其他的优化和修复

## 3.2.2

- 优化了菜单的功能描述，添加了毫秒单位说明
- 修复了 `自定义鼠标托盘提示`
- 修复了变量初始化错误
- 其他的优化和修复

## 3.2.1

- 下拉菜单禁止通过鼠标滚轮切换，避免误切换
- 优化了 exe 版本的更新逻辑
- 修复了配置写入的防抖逻辑缺陷
- 优化了输入光标获取的相关方法
- 其他的优化和修复

## 3.2.0

- 配置的默认值不再写入配置文件，仅在修改配置时写入
- 在 `更多设置` 中添加 `指定窗口自动暂停`
  - 它替换了 `打开软件目录` 的位置
  - 因为 `打开软件目录` 和 `打开数据目录` 没有太大区别
- 优化了版本更新
  - 使用子进程进行版本更新，避免阻塞主进程
  - 移除了 `更新检查间隔时间` 和 `静默更新`，简化为 `启动时检查更新`
- 优化了 `状态提示 - 符号方案` 的菜单布局
  - 合并 `白名单` 和 `黑名单` 为 `白/黑名单`
  - 将 `不同屏幕的基础偏移量` 从 `窗口偏移量` 中移出，并更名为 `屏幕偏移量`
- 其他的优化和修复

## 3.1.6

- 显示器配置变更时更新屏幕数据
- 调整了更新弹窗的日志字体样式
- 修正文档链接路径、文案等
  - 将 `输入法状态的切换方式` 修改为 `输入法状态的切换模式`
  - 将 `窗口特殊偏移量` 简化为 `窗口偏移量`

## 3.1.5

- 修复了多屏幕时，当副屏幕在主屏幕左侧或者上方时浮窗方案显示异常的问题

## 3.1.4

- `指定窗口自动退出` 排除资源管理器窗口，避免在资源管理器中使用时自动退出
- 调整了托盘菜单的菜单项的位置
- 其他的优化和修复

## 3.1.3

- 修复了 `JAB/JetBrains IDE 支持`
- 重构了延迟隐藏的处理逻辑

## 3.1.2

- 修复了 `开机自启动` 和 `以管理员权限运行` 的相关问题
- 其他的优化和修复

## 3.1.0

**新功能**

- 符号方案的窗口偏移量现在支持标题级匹配，允许为单个应用的不同窗口设置窗口偏移量
- 允许符号方案的文本类型符号内容设置为空，表示在该状态下不显示符号
- 允许浮窗方案的文本设置为空，表示在该状态下不显示浮窗

**性能优化**

- 通过预解析窗口规则并使用 Map 映射，大幅提升了窗口规则的匹配性能

**问题修复**

- 修复了符号方案中偏移量未生效的问题
- 修复了浮窗方案和符号方案中透明度失效的问题
- 修复了从 3.0.0 中数据未完整迁移的问题
- 修复了部分配置更改后未及时生效的问题

**其他改进**

- 为配置修改添加防抖，以解决配置未正常写入的问题
- 补充了更多配置项的合法性校验
- 优化了文件下载逻辑和下载界面
- 更新并统一了控件渲染相关方法
- 优化了部分菜单布局
- 其他优化与修复

## 3.0.0

- 新增 `浮窗方案` ，支持自定义样式、位置和过渡动画，为状态提示带来更优雅的视觉体验
- 新增 `颜色选择器`，所有颜色配置项均可通过可视化调色盘设置，无需手动输入色值
- 符号方案中的方块符号升级为 `形状符号`，新增圆角样式，选择「小圆角」后将变为圆形
- 配置菜单全面支持 `i18n` 国际化
- 新增对日文和韩文输入法的状态识别支持
- 新增对美式键盘与中文输入法混用场景下的多键盘布局状态识别支持
- 为鼠标方案支持设置为空，表示在该状态下还原系统默认鼠标样式
- 浮窗方案和所有菜单添加了流畅的过渡动画
- 窗口新增圆角和深色模式标题栏支持
- 为所有配置项添加合法性校验，配置异常时自动回退到默认值
- 优化了窗口规则的查找性能
- 新增鼠标穿透，移除符号悬停隐藏，改为固定实时显示
- 重构了配置文件、数据目录和菜单布局，统一代码风格与命名规范
- AHK 运行环境升级至 v2.0.26
- 版本号从时间回归到 X.Y.Z
- 其他的优化和修复

## [历史版本](./CHANGELOG.archive.md)
